<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CalculatorButton">
        <item name="android:textColor">@color/colorCalculatorButtonsText</item>
        <item name="android:theme" tools:targetApi="lollipop">@android:style/ThemeOverlay.Material.Dark</item>
    </style>

    <style name="CalculatorButton.Memory" parent="CalculatorButton">
        <item name="android:backgroundTint" tools:targetApi="lollipop">@color/colorCalculatorButtonMemory</item>
    </style>

    <style name="CalculatorButton.Number" parent="CalculatorButton">
        <item name="android:backgroundTint" tools:targetApi="lollipop">@color/colorCalculatorButtonNumber</item>
        <item name="android:textAppearance">@android:style/TextAppearance.Large</item>
    </style>

    <style name="CalculatorButton.Action" parent="CalculatorButton">
        <item name="android:backgroundTint" tools:targetApi="lollipop">@color/colorCalculatorButtonAction</item>
        <item name="android:textAppearance">@android:style/TextAppearance.Large</item>
    </style>

</resources>
